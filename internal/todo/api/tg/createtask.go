package tg

import "todo/internal/todo/models"

func Create(task *models.Task) error {
	// client.Send("addr tg bot web server", body = task)
	// if status code == 200 return nil
	// if status code != 200 return err
	return nil
}
